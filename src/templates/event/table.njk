{% macro table(events, tmpl) %}
    {% from "./row.njk" import drawRow %}
    <div class="wsb-schedule">
        <div class="wsb-table" data-event-list>
            <div class="wsb-table__head">
                <div class="wsb-table__col">When</div>
                <div class="wsb-table__col">Where</div>
                <div class="wsb-table__col">What</div>
                <div class="wsb-table__col"></div>
            </div>
            {% for event in events %}
                {% if tmpl %}
                    {{ tmpl(event) | safe }}
                {% else %}
                    {{ drawRow(event) }}
                {% endif %}
            {% endfor %}
        </div>
        <div class="wsb-no-events" data-empty-list>
            No events were found
        </div>
    </div>
{% endmacro %}
